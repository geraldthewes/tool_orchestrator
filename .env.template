# ToolOrchestra Environment Configuration
#
# DEPRECATED: This file is no longer used.
#
# All configuration is now managed via config/config.yaml:
#   1. Copy config/config.yaml.template to config/config.yaml
#   2. Edit config/config.yaml with your actual endpoints and secrets
#   3. For production: run `make push-config` to push to Consul
#
# This file is kept only for reference. Delete it if you don't need it.
